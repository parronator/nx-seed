import React from 'react';

import './list.scss';

/* eslint-disable-next-line */
export interface ButtonProps {}

export const TodoList = (props: ButtonProps) => {
  return (
    <div>
      <h1>Welcome to button!</h1>
    </div>
  );
};

export default TodoList;
